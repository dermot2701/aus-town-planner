steps:
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - app
      - deploy
      - app.yaml
      - --quiet
      - --project=$PROJECT_ID

options:
  logging: CLOUD_LOGGING_ONLY
